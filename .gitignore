# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Reference copies of boepie's code and instruction files. Read-only source
# text for kennis, never part of it.
/reference/

# Internal design notes.
/design/

# Local scratch, sorted by hand.
/archive/

# Agent instruction file, kept local.
CLAUDE.md

# API keys. Never committed - this repository is public, and a key that
# reaches a public remote is burned even if the commit is removed later.
.env
.env.*
*.key
credentials.toml
