# Author a pack

A **pack** is how knowledge reaches kennis from outside: a `.ken.yml` file
declaring what it ships, plus the content it names. It is data and never
code, so a pack cannot run anything - the worst a bad one can do is declare
documents you did not want.

!!! note "Installing works; materialising does not yet"
    `kennis pack init`, `update`, `validate` and `add` are built.
    `pack remove`, `list` and `status` are not, and neither are
    `corpus sync` and `context sync` - the commands that turn an installed
    pack's declarations into documents you can search. So today a pack can
    be written, checked and installed, and its content sits in the store
    rather than in the corpus.

The three commands here are what a **provider** runs, in its own repository
and its own release pipeline. A provider is any tool that wants its
knowledge searchable in kennis.

## Scaffold one

```
kennis pack init --id boepie --name "stimela pipelines"
```

This writes `boepie.ken.yml`. Give a path as an argument to put it
somewhere else.

The `--id` is the pack's identity, and it is stable forever: it names the
pack's directory in kennis's store, it is the key that decides whether a
second `pack add` is an update or a new pack, and it appears in every
document the pack owns as `owner: pack:boepie`. Lowercase letters, digits
and dashes.

The first line of the scaffold is the part worth knowing about:

```yaml
# yaml-language-server: $schema=https://raw.githubusercontent.com/kwazzi-jack/kennis/main/schema/ken-1.json
```

With the `redhat.vscode-yaml` extension, that one comment gives completion
and inline errors while you write the file - **with kennis not installed at
all**. kennis itself never fetches it; `pack validate` uses the copy inside
its own wheel, so checking a pack never needs the network.

`init` refuses to overwrite an existing file. A pack holds declarations
kennis did not write and has no history to restore them from.

## Declare what it ships

Four sections, all optional. Two are **declarations**, fetched over the
network when the pack is installed:

```yaml
corpus:
  literature:
    - citekey: smirnovRevisitingRIMEI2011
      title: "Revisiting the radio interferometer measurement equation I"
      arxiv_id: "1101.1764"
  docs:
    - project: stimela
      base_url: "https://stimela.readthedocs.io/en/latest/"
```

Every literature entry needs a real identifier - an arXiv id, a DOI or an
ADS bibcode - and `validate` refuses without one. The citekey is a handle
for humans; the identifier is the identity, and it is what kennis uses to
tell a renamed citekey from a different paper.

Two are **content**, copied verbatim:

```yaml
corpus:
  notes:
    - source: notes/
      group: stimela
      include: ["**/*.md"]
      exclude: []

context:
  - source: content/
```

`notes` goes into the machine-global corpus; `context` goes into each
project's `.context/` bundle. A `source:` is a directory relative to the
pack file, and it may not leave the pack - neither as a written path nor
through a symlink.

Two sources in the same section may not overlap. kennis addresses a shipped
file by its section, its source and its path within that source, so a file
reachable through two sources would have two addresses and no single answer
to "did this move".

## Record the digests

```
kennis pack update boepie.ken.yml
```

This walks each source, honouring `include` and `exclude`, and writes the
`generated:` block at the end of the file - a digest per shipped file.

```yaml
# Generated by `kennis pack update` - do not hand-edit.
generated:
  at: "2026-09-26T14:47:01Z"
  by: "kennis 0.2.0"
  content:
    notes:
      - source: notes/
        files:
          "conventions.md": "0b692334cb36..."
```

Two things about it:

- **The block must be the last section.** `update` replaces from the marker
  comment to the end of the file, which is how everything above it -
  including your comments and the schema line - survives byte for byte.
  It refuses rather than replacing anything you put below it.
- **A run that changes nothing writes nothing.** Not the same bytes: the
  file is not opened for writing at all. So calling `update` on every
  release build is free, and the pack file's own hash only moves when its
  content really did.

`update` refuses a `source:` naming a directory that is not there. An empty
digest map would be indistinguishable from a pack that ships nothing, and
that claim is one kennis acts on.

## Check it

```
kennis pack validate boepie.ken.yml
```

Non-zero exit on anything wrong, so it belongs in a release pipeline. It
checks four things beyond "does this parse":

| check | what it catches |
|---|---|
| version | a pack written for a newer schema, or needing a newer kennis |
| overlap | two sources in one section that address the same file |
| path hygiene | a file that resolves outside the pack, or a symlink |
| digests | content that changed since `pack update` last ran |

The last one is the reason to run this where the pack is built. kennis
believes a pack's own digests on its fast path, so a pack that ships
without `validate` having passed can lie about its own content and be
believed.

A pack with no `generated:` block is valid - it may simply ship no content -
so `validate` says out loud when it checked no digests, rather than
reporting the same "valid" for two different amounts of checking.

## Install it

```
kennis pack add boepie.ken.yml
```

This is the call a provider makes from its own sync command, on every run.
It does no network I/O: it copies what the pack declares into this
machine's store at `<corpus>/packs/<id>/`, beside the declaration itself
and a `state.json` recording what was applied.

**Installing is not applying.** `pack add` records what a provider
declared; `kennis context sync`, run inside a project, applies the context
half of it to that project. See
[Project knowledge](project-context.md#apply-a-pack).

Calling it repeatedly is cheap. A second `add` of an unchanged pack reports
`Unchanged` and rewrites nothing, which is why a provider can call it
unconditionally.

Three outcomes, and the third is worth knowing about:

| outcome | what happened |
|---|---|
| `Installed` | the pack is new, or the provider shipped a change |
| `Unchanged` | the declaration and the store both check out |
| `Repaired` | the declaration was unchanged and **the store was damaged** |

`Repaired` means kennis found files missing from the store, or holding
something other than their recorded digest, and copied them again. Seeing
it once after an interrupted run is ordinary; seeing it repeatedly is worth
investigating, because something is removing files kennis wrote.

kennis checks the store rather than trusting `state.json` alone for a
specific reason: a store that claims to hold content it does not is
indistinguishable from a pack that ships nothing, and that claim is one the
sync commands would act on.

The store is **not** part of the corpus history. It is a cache of what a
provider last handed over, re-supplied by that provider's next run, so
`corpus init` gitignores `packs/` and `pack add` makes sure of it on a
corpus that predates the feature.

`pack add` re-checks everything `pack validate` checks, including the
digests, and installs nothing if any of it fails. `validate` is the
provider's own check and nothing makes them run it.

## See what is installed

```
kennis pack list
```

One line per pack: its id, its own version, and how many files the store
holds for it. The id is the column you copy out of and hand to
`kennis pack remove`.

```
kennis pack status
```

The same packs with more said about each one, and one thing `list` never
reports: **which packs declare the same item, and which of them owns it.**

```
alpha 1.2.0, 2 files
  = added 2026-09-26T16:25:58Z, last applied 2026-09-26T16:26:55Z
beta 0.4.1, 1 file
  = added 2026-09-26T16:25:59Z
Shared 2 items
  ~ documentation project 'stimela' is declared by alpha and beta; alpha
    owns it, as the earliest installation
  ~ note 'install/setup.md' is declared by alpha and beta; alpha owns it,
    as the earliest installation
```

Two timestamps, because they answer different questions. The first is when
the pack was first added and it never changes; the second is when the
provider last handed it over. A pack showing only one has not been
re-applied since it was installed.

**The owner is the earliest installation, never the most recent.** A
provider calls `pack add` on every run, so deciding by the most recent one
would flip ownership back and forth and rewrite the same documents forever.
The first timestamp is immutable for exactly this reason, so the answer is
stable: re-adding a pack does not make it the owner of anything.

Two packs shipping the same filename into different groups do not collide -
`group:` is part of where a file lands, and so part of what makes two
declarations the same item. Neither do a tree shipped to `corpus.notes` and
the same tree shipped to `context`: they land in different places.

`status` also reports two kinds of damage:

| line | what it means | what to do |
|---|---|---|
| `the stored content does not match what was recorded` | files under `packs/<id>/` are missing or changed | `kennis pack add <path>`, which `status` names when the source file is still there |
| `N directories in the store with no readable state` | a half-written install: a directory with no `state.json` that parses | `kennis pack remove <name>` |

If the pack's source file is gone from this machine, `status` says so and
names the path it was added from. There is nothing local to repair from
then, and the answer is to run the provider that built it again.

## Remove one

```
kennis pack remove boepie
```

Drops that pack from the store: its declaration, its copied content and its
state file.

**It reaches the store and nothing else.** Documents already in the corpus
stay where they are, and so does this pack's content in any project you have
checked out - kennis keeps no registry of your projects, so it cannot reach
into them. Each of those converges when it is next synchronised.

Removing a pack that was never installed fails rather than reporting
success, because the realistic cause is a mistyped id and "removed" would
say the thing you meant is gone.
